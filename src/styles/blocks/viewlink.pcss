.viewlink {
    color: #586ed3;
    display: flex;
    align-items: center;
    font-weight: bold;

    &:hover {
        text-decoration: underline $links-color;
    }
}

.viewlink__icon {
    width: 15px;
    height: 15px;
    fill: currentColor;
    margin-right: 10px;
}